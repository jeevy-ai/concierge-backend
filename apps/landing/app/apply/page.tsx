import { ApplyShell } from "./_components/ApplyShell";

export const metadata = {
  title: "Apply — Jeevy",
  description: "Get early access to Jeevy, your AI concierge. Takes ~2 minutes.",
};

export default function ApplyPage() {
  return <ApplyShell />;
}
